import React from 'react';
import ReactDOM from 'react-dom';
import DashboardPage from './main/js/DashboardPage'; // Check the path to your DashboardPage component

ReactDOM.render(
    <React.StrictMode>
        <DashboardPage />
    </React.StrictMode>,
    document.getElementById('root')
);
